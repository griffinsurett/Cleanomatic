---
const {
  label,
  name,
  placeholder = "",
  value = "",
  required = false,
  rows = 4,
  inputClass = "",
  wrapperClass = "",
  labelClass = "",
} = Astro.props;

// If no label, append "*" into the placeholder
const placeholderText = required && !label
  ? `${placeholder || name} *`
  : placeholder;
---
<div class={wrapperClass}>
  {label && (
    <label for={name} class={labelClass}>
      {label}
      {required && <span class="text-primary ml-1">*</span>}
    </label>
  )}
  <textarea
    id={name}
    name={name}
    placeholder={placeholderText}
    required={required}
    rows={rows}
    class={inputClass}
  >{value}</textarea>
</div>
