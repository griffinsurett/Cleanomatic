---
import { Image } from "astro:assets";
import { SiteData } from "@/content/SiteData";

// Accept optional props to override or extend classes.
const {
  containerClass = "",
  titleClass = "h1",
  taglineClass = "",
  imageClass = "",
} = Astro.props;
---

<a
  href="/"
  class={`flex flex-col xl:items-center justify-center items-start ${containerClass}`}
>
  <div class={`${titleClass}`}>
    {SiteData.title}
  </div>
  <div
    class={`tagline-and-logo w-fit flex justify-center items-center text-stroke h5 font-bold capitalize italic ${taglineClass}`}
  >
    {SiteData.tagline}
    <Image
      src={SiteData.logo.src}
      alt={`${SiteData.title} logo`}
      class={`h-4 w-auto md:h-7 animate-slide-right mx-[var(--spacing-xs)] ${imageClass}`}
      loading="eager"
      fetchpriority="high"
      aria-hidden="true"
    />
  </div>
</a>
