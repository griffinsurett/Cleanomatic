---
const { src, controls = true, autoplay = false, loop = false, className = "" } = Astro.props;
---
<video
  src={src}
  controls={controls}
  autoplay={autoplay}
  loop={loop}
  class={`max-w-full h-auto rounded ${className}`}
>
</video>
